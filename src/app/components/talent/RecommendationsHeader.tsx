'use client';

const Header = () => (
    <header className="bg-blue-600 text-white p-6 sm:p-8 text-center">
        {/* Heading with responsive font size */}
        <h1 className="text-3xl sm:text-4xl font-semibold mb-4">Recommendations</h1>
    </header>
);

export default Header;
